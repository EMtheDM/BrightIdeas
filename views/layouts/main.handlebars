<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BrightIdeas</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fuzzy+Bubbles:wght@700&family=Raleway:ital,wght@1,500&family=Shalimar&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/jass.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div>
        <header>
            <h1>BrightIdeas</h1>

            {{!-- Conditional Navigation Options --}}
            {{!-- If logged in, show logout option. If logged out, show login option. --}}
            <nav class="nav">
                {{#if logged_in}}
                    <a href='/profile'>Profile</a>
                    <button class="btn" id="logout">logout</button>
                {{else}}
                    <a class="login-link" href="/login">login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>
        <footer>

        </footer>
    </div>

{{!-- Render script for logged in users only --}}
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>
</html>